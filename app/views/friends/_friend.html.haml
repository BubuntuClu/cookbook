%div{ id: "friend_#{friend.id}" }
  = link_to "#{friend.email}", user_profile_path(friend)
  = link_to 'Написать сообщение', user_profile_chat_messages_path(friend)
  = link_to 'Убрать из друзей', user_profile_remove_friend_url(friend, type: 'friend_list'), method: :post, remote: true
%br
