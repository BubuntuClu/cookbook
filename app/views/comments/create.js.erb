<% if @comment.errors.present? %>
  <% @comment.errors.full_messages.each do |message| %>
    $('.comment_errors').html('<%= j message %>');
  <% end %>
<% else %>
  $('.user_comments').append("<%= j render 'comments/comment', comment: @comment %>");
  $('#comment_body').val('');
<% end %>
